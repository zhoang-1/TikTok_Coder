import React from 'react';
import Header from '../header/Header';
import Wrapper from './Wrapper';

const Home = () => {
    return (
        <div>
            <Header />
            <Wrapper />
        </div>
    );
};

export default Home;
